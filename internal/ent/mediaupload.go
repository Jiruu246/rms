// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Jiruu246/rms/internal/ent/mediaasset"
	"github.com/Jiruu246/rms/internal/ent/mediaupload"
	"github.com/Jiruu246/rms/internal/ent/user"
	"github.com/google/uuid"
)

// MediaUpload is the model entity for the MediaUpload schema.
type MediaUpload struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// User who requested this upload session; only this user may consume it
	OwnerID uuid.UUID `json:"owner_id,omitempty"`
	// What the resulting asset will be used for
	Purpose mediaupload.Purpose `json:"purpose,omitempty"`
	// Provider-neutral storage object key granted for this session
	ObjectKey string `json:"object_key,omitempty"`
	// failed: the uploaded object was rejected at consume time (e.g. content-type/size mismatch) and cleaned up from storage; terminal, never retried. Expiry is not a distinct status — it is derived from expires_at against the current time
	Status mediaupload.Status `json:"status,omitempty"`
	// Grant deadline; consuming after this time is rejected
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MediaUploadQuery when eager-loading is set.
	Edges        MediaUploadEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MediaUploadEdges holds the relations/edges for other nodes in the graph.
type MediaUploadEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// Asset holds the value of the asset edge.
	Asset *MediaAsset `json:"asset,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MediaUploadEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// AssetOrErr returns the Asset value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MediaUploadEdges) AssetOrErr() (*MediaAsset, error) {
	if e.Asset != nil {
		return e.Asset, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: mediaasset.Label}
	}
	return nil, &NotLoadedError{edge: "asset"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MediaUpload) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mediaupload.FieldPurpose, mediaupload.FieldObjectKey, mediaupload.FieldStatus:
			values[i] = new(sql.NullString)
		case mediaupload.FieldUpdateTime, mediaupload.FieldCreateTime, mediaupload.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		case mediaupload.FieldID, mediaupload.FieldOwnerID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MediaUpload fields.
func (_m *MediaUpload) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mediaupload.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case mediaupload.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				_m.UpdateTime = value.Time
			}
		case mediaupload.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				_m.CreateTime = value.Time
			}
		case mediaupload.FieldOwnerID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value != nil {
				_m.OwnerID = *value
			}
		case mediaupload.FieldPurpose:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field purpose", values[i])
			} else if value.Valid {
				_m.Purpose = mediaupload.Purpose(value.String)
			}
		case mediaupload.FieldObjectKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field object_key", values[i])
			} else if value.Valid {
				_m.ObjectKey = value.String
			}
		case mediaupload.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = mediaupload.Status(value.String)
			}
		case mediaupload.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MediaUpload.
// This includes values selected through modifiers, order, etc.
func (_m *MediaUpload) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the MediaUpload entity.
func (_m *MediaUpload) QueryOwner() *UserQuery {
	return NewMediaUploadClient(_m.config).QueryOwner(_m)
}

// QueryAsset queries the "asset" edge of the MediaUpload entity.
func (_m *MediaUpload) QueryAsset() *MediaAssetQuery {
	return NewMediaUploadClient(_m.config).QueryAsset(_m)
}

// Update returns a builder for updating this MediaUpload.
// Note that you need to call MediaUpload.Unwrap() before calling this method if this MediaUpload
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MediaUpload) Update() *MediaUploadUpdateOne {
	return NewMediaUploadClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MediaUpload entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MediaUpload) Unwrap() *MediaUpload {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MediaUpload is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MediaUpload) String() string {
	var builder strings.Builder
	builder.WriteString("MediaUpload(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("update_time=")
	builder.WriteString(_m.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("create_time=")
	builder.WriteString(_m.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("purpose=")
	builder.WriteString(fmt.Sprintf("%v", _m.Purpose))
	builder.WriteString(", ")
	builder.WriteString("object_key=")
	builder.WriteString(_m.ObjectKey)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MediaUploads is a parsable slice of MediaUpload.
type MediaUploads []*MediaUpload
